<h1>Movies list</h1>
<%@movies = Rental.where(" user_id = ?", params[:id]) %>
<% @movies.each do |mov| %>
	<%@movie = Movie.find(mov.movie_id)%>
	<h4><%=@movie.title%>
	<%= link_to "x" , [mov.user , mov]  ,:method => :delete%></h4>
<% end %>